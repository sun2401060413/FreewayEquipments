using System.Collections;
using System.Collections.Generic;
using UnityEngine;

using sz.network;
using EasyRoads3Dv3;

public class TestCase : MonoBehaviour
{
    //public EasyRoads3Dv3.ERConnectionVecs
    public GameObject obj;
    public ERConnectionVecs _vecs;
    // Start is called before the first frame update
    void Start()
    {
        //_vecs = 
        //obj.TryGetComponent<TwoLaneIndicatorControl>(out var src);
        //Debug.Log("src:" + src.frontCtrlCode);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
